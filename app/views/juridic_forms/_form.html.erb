<%= form_with(model: juridic_form, local: true) do |form| %>
  <% if juridic_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(juridic_form.errors.count, "error") %> prohibited this juridic_form from being saved:</h2>

      <ul>
      <% juridic_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label  "Nom de la forme", class: "control-label" %>
    <%= form.text_field :name, id: :juridic_form_name, class:"form-control", placeholder: "Nom de la forme juridique" %>
  </div>

  <div class="form-group">
    <%= form.label "Description de la forme juridique" %>
    <%= form.text_area :description, id: :juridic_form_description , rows: '7', colos: "10", placeholder: "Description de la forme juridique"%>
  </div>

  <div class="actions">
    <%= form.submit "Enregistrer", class: "btn btn-primary" %>
  </div>
<% end %>
