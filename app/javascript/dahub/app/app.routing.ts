import {ModuleWithProviders} from "@angular/core";
/*
export const routes: Routes = [
    {path: '', redirectTo: '/dahub', pathMatch: 'full'},
    {path: '**', redirectTo: '/dahub/errors/not-found'},
];

export const  appRouting: ModuleWithProviders = RouterModule.forRoot(routes, {userHash: false});

*/