<%= form_with(model: etablissement_activity, local: true) do |form| %>
  <% if etablissement_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(etablissement_activity.errors.count, "error") %> prohibited this etablissement_activity from being saved:</h2>

      <ul>
      <% etablissement_activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :etablissement_id %>
    <%= form.text_field :etablissement_id, id: :etablissement_activity_etablissement_id %>
  </div>

  <div class="field">
    <%= form.label :activity_id %>
    <%= form.text_field :activity_id, id: :etablissement_activity_activity_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
