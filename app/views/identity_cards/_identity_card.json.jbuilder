json.extract! identity_card, :id, :matricule, :start_at, :end_at, :employee_id, :authority_full_name, :authority_title, :country_id, :city_id, :created_at, :updated_at
json.url identity_card_url(identity_card, format: :json)
