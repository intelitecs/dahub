<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label "Nom d'utilisateur", class: 'control-label', for: :user_username %>
    <%= form.text_field :username, id: :user_username, class: "form-control", autocomplete: "off" %>
  </div>

  <div class="form-group">
    <%= form.label  "Adresse Email", class: "control-label", for: :user_username %>
    <%= form.text_field :email, id: :user_email , autocomplete: "off", class: "form-control"%>
  </div>

  <div class="form-group">
    <%= form.label "Mot de passe", class: "control-label" %>
    <%= form.password_field :password, id: :user_password, class: "form-control" %>
  </div>

    <div class="form-group">
      <%= form.label "Confirmation du Mot de passe", class: "control-label" %>
      <%= form.password_field :password_confirmation, id: :user_password_confirmation, class: "form-control" %>
    </div>


    <div class="actions">
      <%= form.submit "Enregistrer", class:"btn btn-primary" %>
    </div>

<% end %>
